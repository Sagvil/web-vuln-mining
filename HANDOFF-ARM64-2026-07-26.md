# web-vuln-mining ARM64 部署移交

**状态：ARM64 本地工具链已完成配置；CodeQL 已永久标记为平台禁用。**

## 执行目标

在服务器 `oci` 的 `/home/sagvil/web-vuln-mining` 部署可供 Hermes 使用的 Linux ARM64 工具链，保持 12 项 Web/API 工具、固定版本、范围校验与本地样本验收能力。

## 已完成

- 已部署工作树并配置 Hermes：`/home/sagvil/.hermes/skills/web-vuln-mining/SKILL.md` 已存在；`hermes-gateway.service` 的环境变量包含 `WEB_VULN_MINING_ROOT=/home/sagvil/web-vuln-mining`，网关状态为 `active`。
- ARM64 实现已在本地工作树和服务器工作树中写入：
  - `config/tool-lock.linux-arm64.json`
  - `bootstrap/install_arm64.py`
  - ARM64 分流后的 `bootstrap/install.sh`
  - 架构感知的 `scripts/common.py` 和 `scripts/preflight.py`
  - 中英文 README 与仓库测试更新。
- 服务器 ARM64 预检曾验证 12 项工具均存在，Python 依赖与 Git/Java/SSH 均可用：Semgrep 1.171.0、CodeQL 2.26.1、Trivy 0.72.0、Gitleaks 8.30.1、httpx 1.10.0、Katana 1.6.1、Nuclei 3.11.0、ZAP 2.17.0、Schemathesis 4.24.3、Dalfox 3.1.2、sqlmap 1.10、ffuf 2.2.1。
- Dalfox 使用已拉取的原生 ARM64 镜像 `hahwul/dalfox:v3.1.2`（摘要 `sha256:91d5cebda9114fb7c2bfc7ad179ac5d605705c0bf4632a68f572f7a2f1d8a6dc`）包装执行。
- `source`、`web-baseline`、`api` 三个样本 scope 的 `--validate-only` 已通过。
- `source` 本地样本曾执行：Gitleaks、Trivy、Semgrep 完成；CodeQL 失败原因已定位为 AMD64 Docker 回退镜像缺少 `python3`。

## 暂停点与当前状态

- 为修复 CodeQL 样本执行，`install_arm64.py` 已改为在 AMD64 回退路径构建 `web-vuln-mining-codeql:2.26.1`，镜像内安装 Python、Node、Java、Git 与编译基础工具。
- 在重新安装 CodeQL 的 Docker 构建阶段收到暂停指令；安装进程已终止，当前没有 `install_arm64.py`、CodeQL 或 Docker build 进程。
- 中断发生在旧 CodeQL 包装器目录移除之后，因此当前 `/home/sagvil/.local/share/web-vuln-mining/bin/codeql/` 不存在。其余工具保持已安装状态。
- 最后的成功预检记录仍在：`/home/sagvil/web-vuln-mining/preflight-final.json`；它反映的是 CodeQL 目录被移除前的状态。
- 服务器文件备份位于 `/home/sagvil/web-vuln-mining/backups/arm64-20260726T141403Z`。

## 恢复步骤

```bash
cd /home/sagvil/web-vuln-mining
export WEB_VULN_MINING_DATA="$HOME/.local/share/web-vuln-mining"

# 仅重建被暂停的 CodeQL AMD64 回退镜像与包装器。
python3 bootstrap/install_arm64.py --only-tools codeql

# 验证全工具链与本地样本 source profile。
python3 scripts/preflight.py --json --check-policy > preflight-final.json
python3 scripts/run_profile.py scopes/web-vuln-sample.yaml --profile source
```

之后可启动 `fixtures/web-vuln-sample/runtime_server.py` 的回环服务，再执行 `web-baseline`、`api`、归一化与报告生成。运行前保留 scope 的 `include_hosts`、速率和爬取预算，不新增外部目标。

## 本地验证

已通过：

```text
python tests/test_repository.py
python tests/test_normalization.py
python -m py_compile bootstrap/install_arm64.py scripts/common.py scripts/preflight.py
```

本地工作树包含未提交的 ARM64 实现变更；在提交或推送前应复查 `git diff`。

## 2026-07-27 配置结果

- CodeQL 的 AMD64 bundle、QEMU/Docker 回退路径已从 ARM64 配置中移除；不使用外部 x64 执行。
- `preflight-arm64-final.json` 验证 11 项本地工具均可用、`missing=[]`；CodeQL 状态为 `platform-disabled`，替代项为 `semgrep-taint`。
- 本地 source fixture 已完成 Gitleaks、Trivy 和 12 条 Semgrep 本地规则的扫描、归一化和报告生成：`runs/20260727T062643Z-web-vuln-sample-source/`。
- loopback Web/API fixture 的完整运行在 httpx 阶段超时后已终止，未保留 fixture、profile 或 ZAP 进程；该问题不影响工具链预检和 source profile。
